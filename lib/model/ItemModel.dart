class ItemModel {
  // class constructor
  ItemModel(this.id, this.image, this.title, this.description,this.isCheck);

  // class fields
  final int id;
  final String image;
  final String title;
  final String description;
  final bool isCheck;
}